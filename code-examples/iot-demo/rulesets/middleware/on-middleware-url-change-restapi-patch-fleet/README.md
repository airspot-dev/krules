# on-middleware-url-change-restapi-patch-fleet

When a knative service (both middleware endpoint and dashboard) becames available exposing his own URL the corresponding
model is updated via a REST API call
