import os

import setuptools
from setuptools import setup, find_packages


def read(fname):
    return open(os.path.join(os.path.dirname(__file__), fname)).read()

setup(
    name='krules-dispatcher-cloudevents',
{% if release_version %}
    version="{{ release_version }}",
{%  endif %}
    author="Alberto Degli Esposti",
    author_email="alberto@arispot.tech",
    description="KRules cloudevents PUBSUB dispatcher",
    license="Apache Licence 2.0",
    keywords="krules cloudevents pubsub router",
    url="https://github.com/airspot-dev/krules-dispatcher-cloudevents-pubsub",
    packages=setuptools.find_packages(),
    long_description=read('README.md'),
    classifiers=[
        "Development Status :: 3 - Alpha",
        "License :: OSI Approved :: Apache Software License",
    ],
    install_requires=[
{% if release_version %}
        'krules-core=={{ release_version }}',
{% endif %}
        'cloudevents==1.7.1',
        'google-cloud-pubsub==2.13.11',
    ],
    setup_requires=[
        'pytest-runner',
    ],
    tests_require=[
        'pytest',
        'pytest-localserver',
    ],
)
